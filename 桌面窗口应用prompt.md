请在当前工作目录中创建一个使用 C# + WPF 的 Windows 桌面应用项目。请直接创建文件并完成初始化，不要只提供示例代码或操作步骤。

## 一、项目初始化要求

1. 先检查当前目录、已有项目结构、Git 状态以及可用的 .NET SDK：
   - 如果当前目录中已有项目，优先沿用现有结构和配置，不要覆盖、删除或重建已有内容。
   - 如果尚无项目，创建一个结构清晰、能够独立构建和启动的最小 WPF 应用。
   - 选择与当前环境兼容且仍受支持的稳定版 .NET；未指定项目名称时，使用合理的名称，并在完成后说明。

2. 初始化必要的项目文件，包括：
   - 解决方案和项目文件。
   - 应用入口与主窗口。
   - 适合 C# / WPF 项目的 `.gitignore`。
   - 简洁的 `README.md`，说明项目用途、环境要求、构建和启动方式。

3. 主窗口应能够正常显示，并包含简单的界面内容和至少一个可验证的交互，例如点击按钮后更新一段文本。未明确要求的业务功能不要自行扩展。

4. 优先使用平台内置能力，不要为了初始化项目引入不必要的第三方依赖或复杂架构。

## 二、初始化 AGENTS.md

在项目根目录创建 `AGENTS.md`，写入下方规则，作为后续开发和修复任务的持续协作约定。

如果该文件已经存在，请先阅读，在保留现有有效规则的基础上合并补充，不要直接覆盖；发现实质性冲突时应明确说明。

AGENTS.md 内容如下：

```markdown
# 版本管理

使用 Git 管理本仓库的源代码。

## 仓库初始化

- 开始工作前，检查当前目录是否已经位于 Git 工作树中。
- 如果已有 Git 仓库，沿用现有仓库，不要重复初始化或创建嵌套仓库。
- 如果尚未初始化 Git，在项目根目录执行初始化。
- 配置适合本项目的 `.gitignore`，排除构建产物、IDE 缓存、临时文件以及不应进入版本管理的本地配置和敏感信息。

## 自动提交

- 在开发功能、修复缺陷或进行必要重构时，每完成一个小粒度、逻辑完整且经过适当验证的进展，就自动创建一个 commit。
- 提交粒度以一个独立、可解释、便于回溯的改动为准；不要将多个无关改动混在一起，也不要机械地按文件或代码行数拆分提交。
- 项目初始化完成并验证后，也应创建一次提交。
- 提交前检查 Git 状态与变更内容，只暂存和提交本次任务相关的修改。不要将用户已有的改动或其他无关文件一并提交。
- 提交信息应准确说明变更，建议使用 `feat:`、`fix:`、`refactor:`、`docs:`、`chore:` 等前缀。
- 创建本地 commit 无需事先征求用户许可。提交成功后，简短说明提交内容及 commit 短哈希。
- 未经用户明确要求，不执行远程推送，不改写已有提交历史，不使用可能丢失现有改动的破坏性 Git 操作。
- 如果因为缺少 Git 身份配置、权限或其他环境问题而无法提交，保留已有成果并说明阻塞原因；不要编造身份、擅自修改全局 Git 配置或声称提交成功。

# 代码原则

## 模块与文件拆分

- 按职责组织模块和文件，保持清晰的边界与合理的依赖关系。
- 随着代码规模增长，主动检查是否需要拆分模块、类和文件，避免大量不相关逻辑集中在同一处。
- 优先根据职责、复杂度和可维护性决定是否拆分，不机械地按照代码行数拆分，也不为了拆分而制造过度抽象。
- 修改现有代码时，保持与项目现有命名、格式和组织方式一致，避免与当前任务无关的大规模重构。

## WPF 开发约定

- 合理分离界面、状态和业务逻辑；根据项目复杂度采用 MVVM 等适当的组织方式，避免过度设计。
- 不在窗口的 code-behind 中堆积业务逻辑；仅与视图直接相关的少量逻辑可以保留。
- 对必要的异常情况进行处理，避免在 UI 线程执行长时间阻塞操作。
- 仅在确有必要时添加第三方依赖，并说明用途。

# 验证与反馈

- 每个准备提交的进展都应进行与变更类型匹配的检查，例如构建、测试或静态检查。
- 如果引入了构建或测试失败，应先修复再提交；如果失败来自既有问题或环境限制，应明确说明原因和影响。
- 无法验证的内容必须明确标注，不得把“已创建文件”“构建成功”和“实际启动验证通过”混为一谈。
- 完成任务后，简要说明实现内容、验证结果、提交记录，以及仍存在的限制或待处理事项。
```

## 三、执行与验收

- 按照 `AGENTS.md` 中的规则完成本次初始化。
- 尝试构建项目；如果当前环境支持运行 WPF，再检查窗口启动和基本交互。
- 如果当前环境无法构建或运行 WPF，仍应完成能够完成的文件创建和检查，并明确说明未验证的部分及原因，不得声称已经运行成功。
- 对未指定且不影响安全的细节，采用合理默认值继续执行；遇到可能覆盖用户内容、丢失改动或影响现有项目的重要冲突时，先说明问题，不要擅自处理。

完成后请简要提供：
1. 创建或修改了哪些主要文件。
2. 如何构建和启动应用。
3. 实际执行了哪些验证及其结果。
4. 已创建的 commit 短哈希和提交说明。
5. 尚未解决的问题或环境限制；没有则无需列出。